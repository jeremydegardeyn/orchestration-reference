# Runtime deps for DAGs (most are preinstalled in the Composer image; pin what the patterns need).
# These mirror the pypi_packages set in Terraform's software_config.
google-cloud-dataform>=0.6.2
google-cloud-dataflow-client>=0.9.0
croniter>=2.0
pytz>=2024.1

# Build-time only (CI runs dag_builder.py before rsync):
jinja2>=3.1
pyyaml>=6.0
